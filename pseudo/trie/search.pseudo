input(value)

Node temp = this->root
for i in [0, size(key) - 1]:
    index = key[i]
    if temp->child[index] == NULL:
        return NOT_FOUND
    temp = temp->child[index]
if temp and pTemp->isEndOfWord: return FOUND