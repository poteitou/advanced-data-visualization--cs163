input(key)
check condition (height, width, ...)
Node temp = this->root
for i in [0, size(key) - 1]:
    index = key[i]
    if temp->child[index] == NULL:
        temp->child[index] = newNode()
    temp = temp->child[index]
temp->isEndOfWord = true