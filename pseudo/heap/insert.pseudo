input(value)
check insert condition (size, duplicate)
push_back(value) to this Heap
i = this->size() - 1
while i != 0 and
this->arr[i] < this->arr[(i - 1) / 2]:
    swap(this->arr[i], this->arr[(i - 1) / 2])
    i = (i - 1) / 2
                                                