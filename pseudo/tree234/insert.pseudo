Node temp = new Node(value)
Check insert condition (duplicate, height...)
Node curNode = root
while not curNode->isLeaf():
    if curNode->isFull():
        split(curNode)
    else: 
        cur = cur->child
add(temp) to curNode